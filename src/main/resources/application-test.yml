spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    enabled: false

app:
  log-path: "./logs-test"
  storage:
    empresa:
      logo-path: "./storage-test/logo/"

  required: